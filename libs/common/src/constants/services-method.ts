export enum USER_SERVICE_METHOD {
  FIND_BY_ID = 'findById',
  FIND_BY_EMAIL = 'findByEmail',
  MATCH_PASSWORD = 'matchPassword',
}

export enum Auth_SERVICE_METHOD {
  AUTHENTICATE = 'authenticate',
}
